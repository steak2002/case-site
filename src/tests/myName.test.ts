import { describe, it, expect } from "vitest";

describe('class/File', () => {
    // Your tests go here
    describe('function', () => {
        it('should work as expected', () => {

            expect(1 + 1).toBe(2);

        });
    });
});
